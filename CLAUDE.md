# 交通助理（Transportationtool）

航空地勤 / 出差旅行用的一站式工具站，Flask + Vanilla JS，iOS 毛玻璃風格 UI，行動裝置優先（393px）。

## Agent skills

### Issue tracker

Issues 存放在 GitHub Issues（`twwone/Transportationtool`），透過 `gh` CLI 操作。詳見 `docs/agents/issue-tracker.md`。

### Triage labels

使用預設的五種 triage 標籤（`needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`）。詳見 `docs/agents/triage-labels.md`。

### Domain docs

Single-context 佈局：根目錄的 `CONTEXT.md` + `docs/adr/`（尚未建立，由 `/domain-modeling` 視需要延遲建立）。詳見 `docs/agents/domain.md`。
